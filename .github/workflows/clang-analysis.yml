name: clang-analysis

on:
  push:
    branches: [ "main", "ci/**", "feat/**", "test/**", "fix/**" ]
  pull_request:
    branches: [ "main", "ci/**", "feat/**", "test/**", "fix/**" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  clang-tidy:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build clang clang-tidy clang-tools \
            libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev \
            xvfb mesa-utils libgl1-mesa-dri libgl1-mesa-dev

      - name: Configure (clang-tidy)
        run: |
          cmake -S . -B build-clang-tidy -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_CXX_CLANG_TIDY=clang-tidy \
            -DNFX_SILICON_BUILD_TESTS=OFF \
            -DNFX_SILICON_BUILD_SAMPLES=ON \
            -DNFX_SILICON_ENABLE_NATIVE_OPTIMIZATIONS=OFF \
            -DNFX_SILICON_WARNINGS_AS_ERRORS=ON

      - name: Build (runs clang-tidy)
        run: cmake --build build-clang-tidy --parallel

  scan-build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build clang clang-tidy clang-tools \
            libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev \
            xvfb mesa-utils libgl1-mesa-dri libgl1-mesa-dev

      - name: Configure (scan-build)
        run: |
          cmake -S . -B build-scan-build -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DNFX_SILICON_BUILD_TESTS=OFF \
            -DNFX_SILICON_BUILD_SAMPLES=ON \
            -DNFX_SILICON_ENABLE_NATIVE_OPTIMIZATIONS=OFF \
            -DNFX_SILICON_WARNINGS_AS_ERRORS=ON

      - name: Run scan-build
        run: scan-build --status-bugs cmake --build build-scan-build --parallel
